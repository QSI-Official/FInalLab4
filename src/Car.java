import java.awt.*;
public abstract class Car extends Vehicle {
    public Car(int nrDoors, double enginePower, Color color, String modelName, double initX, double initY) {
        super(nrDoors, enginePower, color, modelName, initX, initY);
    }
}